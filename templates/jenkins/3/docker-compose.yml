jenkins:
    image: rakesh635/jenkins:2.60.3
    container_name: jenkins
    restart: always
    ports:
        - "${PORT}:8080"
    environment:
        - PLUGINS=${PLUGINS}
    labels:
        test.plugins: ${PLUGINS}
    command: bash -c "startup.sh"